// Shared goblin visual rig — now the pot goblin model (src/pot-goblin-rig.js): a
// faceted cauldron body, wavy rim collar, leaf cap, and glowing eyes, still built
// with the same installGoblinRig(THREE) contract (materials/makeGoblinRig/marker
// update/glow/shatter helpers, identical return keys) so enemies.js and
// arena-enemies.js need no changes. The old capsule goblin lives in git history.
export { installPotGoblinRig, installGoblinRig } from './pot-goblin-rig.js';
