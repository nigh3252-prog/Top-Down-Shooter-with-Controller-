export const WARDEN_TRIAL_SETTINGS = Object.freeze({
  viewScale: 3,
  spawnRadiusMin: 18,
  spawnRadiusMax: 30,
  enemyHeight: 4.6,
  enemyRadius: 0.68,
  screenMargins: Object.freeze({
    left: 0.08,
    right: 0.08,
    top: 0.22,
    bottom: 0.1,
  }),
});
